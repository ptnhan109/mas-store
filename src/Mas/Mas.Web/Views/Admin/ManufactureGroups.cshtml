
@{
    ViewData["Title"] = "CustomerGroups";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="page-title">
    <div class="row">
        <div class="col-12 col-md-6 order-md-1 order-last">
            <h3>NHÓM KHÁCH HÀNG</h3>
            <p class="text-subtitle text-muted">Nhóm nhà cung cấp</p>
        </div>
        <div class="col-12 col-md-6 order-md-2 order-first">
            <nav aria-label="breadcrumb" class="breadcrumb-header float-start float-lg-end">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="home.html">Trang chủ</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Danh sách nhà cung cấp</li>
                </ol>
            </nav>
        </div>
    </div>
</div>
<div class="page-content">

    <section class="section">
        <div class="col-12 d-flex justify-content-end" style="margin-bottom: 7px;">
            <a href="javascript:;" class="btn btn-success me-1 mb-1" id="add-manufacture-group">
                <i class="bi bi-lock-open"></i> Thêm mới
            </a>
        </div>

        <div class="card">
            <div class="card-body">
                <table class="table">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Tên nhóm</th>
                            <td>Mô tả</td>
                            <td>Số lượng</td>
                            <th>Ngày tạo</th>
                        </tr>
                    </thead>
                    <tbody id="customer-groups">
                    </tbody>
                </table>
            </div>
        </div>

    </section>
</div>

<div class="modal fade text-left modal-" id="modal-add-manufacture-group" tabindex="-1"
     role="dialog" aria-labelledby="myModalLabel1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-scrollable modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="discount-title" style="text-transform: uppercase;">THÊM MỚI NHÓM NHÀ CUNG CẤP</h5>
                <button type="button" class="close rounded-pill" data-bs-dismiss="modal"
                        aria-label="Close">
                    <i data-feather="x"></i>
                </button>
            </div>
            <div class="modal-body">
                <form class="form form-vertical">
                    <div class="form-body">
                        <div class="row">
                            <div class="col-12">
                                <div class="form-group">
                                    <label for="manufacture-group-name">Tên nhóm</label>
                                    <input type="text" id="manufacture-group-name" class="form-control" name="fname" placeholder="Tên danh mục">
                                </div>
                            </div>
                            <div class="col-12">
                                <div class="form-group">
                                    <label for="manufacture-group-description">Mô tả</label>
                                    <textarea class="form-control" id="manufacture-group-description" rows="4"></textarea>
                                </div>
                            </div>

                        </div>
                    </div>
                </form>
            </div>
            <div class="modal-footer pd-t-0">
                <button type="button" class="btn btn-light-primary" data-bs-dismiss="modal">
                    <i class="bx bx-x d-block d-sm-none"></i>
                    <span class="d-none d-sm-block">Hủy bỏ</span>
                </button>
                <button type="button" class="btn btn-primary ml-1">
                    <i class="bx bx-check d-block d-sm-none"></i>
                    <span class="d-none d-sm-block" id="btn-add-manufacture-group">Thêm mới</span>
                </button>
            </div>
        </div>
    </div>
</div>

<div class="modal fade text-left modal-" id="modal-update-manufacture-group" tabindex="-1"
     role="dialog" aria-labelledby="myModalLabel1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-scrollable modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <input id="update-manufacture-id" hidden />
                <h5 class="modal-title" id="discount-title" style="text-transform: uppercase;">THÊM MỚI NHÓM KHÁCH HÀNG</h5>
                <button type="button" class="close rounded-pill" data-bs-dismiss="modal"
                        aria-label="Close">
                    <i data-feather="x"></i>
                </button>
            </div>
            <div class="modal-body">
                <form class="form form-vertical">
                    <div class="form-body">
                        <div class="row">
                            <div class="col-12">
                                <div class="form-group">
                                    <label for="update-name">Tên nhóm</label>
                                    <input type="text" id="update-manufacture-name" class="form-control" name="fname" placeholder="Tên danh mục">
                                </div>
                            </div>
                            <div class="col-12">
                                <div class="form-group">
                                    <label for="update-description">Mô tả</label>
                                    <textarea class="form-control" id="update-manufacture-description" rows="4"></textarea>
                                </div>
                            </div>

                        </div>
                    </div>
                </form>
            </div>
            <div class="modal-footer pd-t-0">
                <button type="button" class="btn btn-light-primary" data-bs-dismiss="modal">
                    <i class="bx bx-x d-block d-sm-none"></i>
                    <span class="d-none d-sm-block">Hủy bỏ</span>
                </button>
                <button type="button" class="btn btn-primary ml-1">
                    <i class="bx bx-check d-block d-sm-none"></i>
                    <span class="d-none d-sm-block" id="btn-update-manufacture-group">Cập nhật</span>
                </button>
            </div>
        </div>
    </div>
</div>
<script src="/assets/vendors/jquery/jquery.min.js"></script>
<script src="/assets/vendors/jquery-datatables/jquery.dataTables.min.js"></script>
<script src="/assets/vendors/jquery-datatables/custom.jquery.dataTables.bootstrap5.min.js"></script>
<script src="/assets/vendors/fontawesome/all.min.js"></script>
<script>
    $(document).ready(function () {

        GetManufactureGroups();

        $("#add-manufacture-group").click(function () {
            $("#modal-add-manufacture-group").modal("show");

        });

        $("#btn-add-manufacture-group").click(function () {
            let name = $("#manufacture-group-name").val();
            let description = $("#manufacture-group-description").val();
            let url = "@Url.Action("AddManufactureGroup", "Admin")";
            let request = {
                Name: name,
                Description: description
            };

            $.ajax({
                url: url,
                type: "POST",
                dataType: "json",
                contentType: "application/json; charset=utf-8",
                traditional: true,
                data: JSON.stringify(request),
                success: function (data) {
                    GetManufactureGroups();
                    $("#modal-add-manufacture-group").modal("hide");
                    showMessage("success", data);
                }
            });

        });

        $("#btn-update-manufacture-group").click(function () {

            let name = $("#update-manufacture-name").val();
            let description = $("#update-manufacture-description").val();
            let id = $("#update-manufacture-id").val();
            let request = {
                Id: id,
                Description: description,
                Name: name
            };

            let url = "@Url.Action("UpdateManufactureGroup", "Admin")";
                $.ajax({
                    url: url,
                    type: "POST",
                    dataType: "json",
                    contentType: "application/json; charset=utf-8",
                    traditional: true,
                    data: JSON.stringify(request),
                    success: function (data) {
                        GetManufactureGroups();
                        $("#modal-update-manufacture-group").modal("hide");
                        showMessage("success", data);
                    }
                });
        });
    });
    function GetManufactureGroups() {
        let url = "@Url.Action("GetManufactureGroups", "Admin")";
        $.ajax({
            url: url,
            type: "GET",
            dataType: "json",
            contentType: "application/json; charset=utf-8",
            traditional: true,
            success: function (data) {
                let html = "";
                let i = 1;
                data.forEach(function (group, index) {
                    html += '<tr group-id="';
                    html += group.id;
                    html += '"><td>';
                    html += i;
                    html += '</td><td>';
                    html += group.name;
                    html += '</td><td>';
                    html += group.description;
                    html += '</td><td>';
                    html += group.quantity;
                    html += '</td><td>';
                    html += group.createdAt;
                    html += '</td><td>';
                    html += '<a href="javascript:;" class="group-update" group-id="';
                    html += group.id;
                    html += '"><i class="fa fa-edit text-primary"></i></a>&nbsp;';
                    html += '<a href="javascript:;" class="group-delete" group-id="';
                    html += group.id;
                    html += '"><i class="fa fa-trash text-danger"></i></a>';
                    html += '</td>';
                    html += '</tr>';
                    i++;
                });

                $("#customer-groups").html(html);

                $("a.group-update").click(function () {
                    let row = $(this).closest("tr");
                    let id = row.attr("group-id");
                    let name = row.find("td:nth-child(2)").text();
                    let description = row.find("td:nth-child(3)").text();

                    $("#update-manufacture-name").val(name);
                    $("#update-manufacture-description").val(description);
                    $("#update-manufacture-id").val(id);
                    $("#modal-update-manufacture-group").modal("show");
                });

                $("a.group-delete").click(function () {
                    let row = $(this).closest("tr");
                    let id = row.attr("group-id");
                    row.remove();
                    DeleteGroup(id);
                });
            }
        });
    }

    function DeleteGroup(id) {
        let url = "@Url.Action("DeleteManufactureGroup", "Admin")";
        $.ajax({
            url: url,
            type: "GET",
            dataType: "json",
            contentType: "application/json; charset=utf-8",
            traditional: true,
            data: {
                id: id
            },
            success: function (data) {
                showMessage("danger", data);
            }
        })
    }

</script>